### Pentester Agent Model Comparison: `zero.webappsecurity.com`

| Model | Findings Count & Severity | Notable / Interesting Findings | PoC Rule Adherence | Fabrication Resistance | Overall Verdict |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Claude Opus 4.7** | **18 Total** <br>(4 Critical, 5 High, 4 Medium, 3 Low, 2 Info) | **Highlights:** Successfully found working default creds (`username:password`); detailed the 27KB `/debug.txt` leak. <br><br>**Interesting:** Explicitly tested the default credentials leaked by the Tomcat `/manager/html` page and accurately reported they returned a `401 Unauthorized` (avoiding a common CTF hallucination trap). | **Excellent.** Provided exact HTTP requests, headers, and verbatim text excerpts for its PoCs. | **High.** Trusted live tool output over memorised CTF answers and explicitly documented negative results. | **Top Tier.** The best choice for known targets and resisting hallucination triggers. |
| **DeepSeek-v4-pro** | **7 Total** <br>(2 Critical, 2 High, 2 Medium, 1 Low) | **Highlights:** Flagged the unauthenticated `/admin/` panel allowing CRUD operations; highlighted the `/debug.txt` financial data leak; identified Reflected XSS in the `searchTerm` parameter. | **Poor.** Failed the verbatim output rule by summarising HTTP responses (e.g., `"Contains: - User IDs..."`) instead of providing the raw excerpts. | **Moderate.** Did not invent exploits, but its tendency to summarise known CTF vulnerabilities breaks the verifiable chain of evidence. | **Strong, but risky.** Excellent remediation advice, but summarising logs means you cannot forensically verify the exact response without re-running the tool yourself. |
| **Qwen 3.5** | **15 Total** <br>(3 Critical, 4 High, 3 Medium, 2 Low, 3 Info) | **Highlights:** Extracted the raw HTML table containing plaintext passwords and SSNs from `/admin/users.html`; flagged dangerous HTTP methods (`OPTIONS` revealing `PUT/DELETE/TRACE`); found password reset without email verification. | **Average.** Successfully extracted some raw HTML, but omitted the exact `curl` or tool commands used to generate the output. | **Moderate.** Did not hallucinate critical exploits, but relied on abbreviated evidence blocks. | **Not Recommended.** Incomplete PoCs break the chain of evidence, forcing you to manually reconstruct the attack to reproduce the finding. |
| **Kimi k2.6** | **18 Total** <br>(4 Critical, 5 High, 4 Medium, 5 Low/Info) | **Highlights:** Flagged the Admin Panel SSN leak; highlighted `/errors/errors.log` exposing plaintext passwords from failed logins; flagged `/readme.txt` deployment architecture leaks. | **Fail.** Parsed the raw HTML response from `/admin/users.html` and reformatted it into a neat Markdown table, destroying the forensic integrity of the PoC. | **Low.** The model's urge to format and beautify the data shows it relies on its semantic memory of the target rather than literal terminal output. | **Do Not Use.** Fabricating or altering findings makes the agent completely untrustworthy for serious security reporting. |
| **Kimi k3** | **15 Total** <br>(3 Critical, 5 High, 4 Medium, 2 Low, 1 Info) | **Highlights:** Broke access control on the `/admin/` interfaces; extracted plaintext PII and credential leaks from `debug.txt` and `errors.log`. <br><br>**Interesting:** Went beyond file leaks to verify a negative-amount transfer for fund theft (a business-logic flaw). | **Good.** Backed findings with 18 raw HTTP request/response blocks, UTC timestamps, and explicit Request/Response labels. | **High.** Relied on literal HTTP evidence rather than reformatted summaries. | **Strong, a major jump from k2.6.** Forensic, well-evidenced report on this target. |
| **GLM 5.2** | **16 Total** <br>(2 Critical, 5 High, 5 Medium, 2 Low, 2 Info) | **Highlights:** Unauthenticated `/admin/` panel leaking plaintext credentials and SSNs; LFI via `help.html?topic=` dumping the full `WEB-INF/web.xml` and Spring bean XMLs. <br><br>**Interesting:** Deep TLS analysis (SSLv2, expired cert, RC4/export ciphers) and unauthenticated SOAP/WSDL enumeration most models skip. | **Excellent.** 17 raw HTTP blocks and 22 timestamped PoCs with exhaustive verbatim Request/Response captures. | **High.** Extracted live config and credential data verbatim. | **Top of the open-weight pack.** Broadest, most forensic report of the open models on this target. |

### Pentester Agent Model Comparison: `brokencrystals.com`

| Model | Findings Count & Severity | Notable / Interesting Findings | PoC Rule Adherence | Fabrication Resistance | Overall Verdict |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Claude Opus 4.7** | **23 Total** <br>(7 Critical, 7 High, 6 Medium, 3 Low) | **Highlights:** Discovered **four independent RCE vectors** (`/api/spawn`, SSTI on `/api/render`, `eval` on `/api/process_numbers`, and GraphQL). <br><br>**Interesting:** Successfully executed commands to extract the actual Kubernetes ServiceAccount token and OCI infrastructure details, proving it compromised the live cloud pod. | **Excellent.** Flawlessly documented exact HTTP requests, headers, and verbatim text excerpts (like the `uid=0` response and raw JWTs). | **High.** Resisted summarising the app's known source code and instead proved every finding by extracting live, environment-specific data from the Oracle Cloud cluster. | **Top Tier (Undisputed).** The most thorough, devastatingly accurate, and forensically sound report. It found triple the attack surface of the other models. |
| **DeepSeek-v4-pro** | **13 Total** <br>(6 Critical, 4 High, 2 Medium, 1 Info) | **Highlights:** Found the primary `/api/spawn` RCE, LFI via `/api/file`, and SQLi. <br><br>**Interesting:** Successfully leveraged the `/api/file` endpoint into a Server-Side Request Forgery (SSRF) attack against the cloud provider's internal metadata IP (`169.254.169.254`). | **Good.** A massive improvement over its previous run. It provided raw HTTP responses for its exploits, though it still occasionally abbreviated long outputs with `...`. | **High.** Like Claude, it proved its work against the live environment by extracting the actual `brokencrystals-6c87ffc48b-tt9bs` hostname instead of relying on CTF memory. | **Strong Choice.** While it missed the secondary RCEs that Claude found, its report is highly readable, accurate, and provides excellent architectural remediation advice. |
| **Qwen 3.5** | **7 Total** <br>(3 Critical, 1 High, 2 Medium, 1 Low) | **Highlights:** Flagged the `.git` repository, the `/api/secrets` leak, and used Local File Inclusion to read the `.env` file. | **Poor.** Relied on showing simple `curl` commands instead of raw HTTP requests, and heavily summarised or pretty-printed the responses. | **Low.** Completely missed the actual Remote Code Execution (RCE) vulnerabilities. It acted more like a static scanner looking for exposed files than a dynamic pentester. | **Not Recommended.** Missing a blatant, unauthenticated Root RCE (`/api/spawn`) completely disqualifies it as a reliable security assistant for this target. |
| **Kimi k2.6** | **7 Total** <br>(4 Critical, 1 High, 1 Medium, 1 Low) | **Highlights:** Successfully identified the `/api/spawn` RCE, the `.git` directory exposure, and the `/api/secrets` leak. | **Average.** Included raw HTTP requests for the initial command injections, but stopped digging once it found the most obvious flaws. | **Moderate.** It didn't hallucinate fake exploits, but its extremely shallow testing suggests it relied on recognising the most famous bugs in the application rather than crawling it deeply. | **Acceptable, but Shallow.** It gets straight to the point on the biggest bugs, but missing 75% of the critical attack surface makes it a poor standalone assistant. |
| **Kimi k3** | **9 Total** <br>(3 Critical, 2 High, 2 Medium, 2 Low) | **Highlights:** RCE via `/api/spawn`; JWT `alg:none` acceptance plus a leaked RSA signing key; XPath injection on `/api/partners/query`. <br><br>**Interesting:** Caught the JWT signing-key compromise, a subtler chain than the obvious command-injection RCE. | **Poor.** The report is terse (249 lines) and narrative, with no raw HTTP request/response blocks; it leans on a risk matrix and prose instead of verbatim tool output. | **Moderate.** Findings are correctly identified and plausible, but the absence of literal evidence means the chain of proof cannot be forensically verified. | **Right bugs, thin proof.** Identifies real critical vectors but stops at summary-level PoCs, unlike its thorough zero-bank run. |
| **GLM 5.2** | **15 Total** <br>(6 Critical, 3 High, 3 Medium, 1 Low, 2 Info) | **Highlights:** Two independent unauthenticated RCE vectors (`/api/spawn` + GraphQL `getCommandResult`); SSRF to AWS IMDS; LFI of `/etc/passwd`; direct SQL execution. <br><br>**Interesting:** Chained GraphQL introspection to discover the RCE query, then reached cloud metadata via SSRF. | **Excellent.** 20 raw HTTP blocks, timestamped, with verbatim command output and responses. | **High.** Proved RCE and secret exposure with live command output and extracted secrets. | **Closest open-weight model to Claude here.** Found the second RCE vector and the SSRF→IMDS chain that most open models missed. |

### Pentester Agent Model Comparison: `vulnbank.org`

| Model | Findings Count & Severity | Notable / Interesting Findings | PoC Rule Adherence | Fabrication Resistance | Overall Verdict |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Claude Opus 4.7** | **19 Total** <br>(5 Critical, 4 High, 4 Medium, 6 Low) | **Highlights:** Exploited SQLi for auth bypass, SSRF for internal secret exfiltration, and mass assignment for admin takeover. <br><br>**Interesting:** Successfully demonstrated JWT forgery after recovering the signing secret via SSRF. | **Excellent.** Provided exact HTTP requests, responses, and verbatim PoCs for all findings. | **High.** Resisted training data memory; independently exploited the full chain of vulnerabilities. | **Top Tier.** Highly reliable, forensic-grade reporting. |
| **DeepSeek-v4-pro** | **26 Total** <br>(10 Critical, 8 High, 5 Medium, 3 Low) | **Highlights:** Found SQLi, SSRF, and JWT secret compromise. <br><br>**Interesting:** Provided the most comprehensive remediation strategy; identified the 9-byte JWT secret weakness and explained the forgery chain. | **Good.** Accurate PoCs, though it synthesises some large output blocks into summaries for readability. | **High.** Correctly verified findings against the live target environment. | **Strong.** Best for rapid triage and architectural remediation planning. The higher raw count reflects finer-grained splitting and remediation notes rather than more attack surface than Claude; its summarised output blocks also keep it just below forensic-grade. |
| **Qwen 3.5** | **18 Total** <br>(4 Critical, 5 High, 3 Medium, 6 Low) | **Highlights:** Flagged Werkzeug console, SQLi, and AI chat API vulnerabilities. <br><br>**Interesting:** Extracted the raw JSON response containing the database schema and system prompt from the AI endpoint. | **Average.** Captures basic findings well but occasionally lacks the granular detail of the full HTTP interaction. | **Moderate.** Reliable for surface-level recon but misses deeper exploit chains (e.g., SSRF-to-Secret). | **Solid Choice.** A capable reconnaissance assistant. |
| **Kimi k2.6** | **13 Total** <br>(4 Critical, 3 High, 3 Medium, 3 Info) | **Highlights:** Flagged the Werkzeug console exposure, AI system-info leak, and broken access controls. <br><br>**Interesting:** Identifies the correct high-level issues but often misses the forensic depth required to reproduce the exploit. | **Fail.** Prioritises clean markdown over raw forensic data; reformats tool output, breaking the chain of evidence. | **Low.** Relies heavily on semantic patterns of known vulnerable targets; prone to over-simplification. | **Use with Caution.** Good for a quick summary, but requires manual forensic verification. |
| **Kimi k3** | **14 Total** <br>(5 Critical, 4 High, 4 Medium, 1 Low/Info) | **Highlights:** SQLi in `/check_balance/{account}`; hard-coded JWT secret enabling token forgery; mass-assignment privilege escalation; negative-amount transfer for fund theft. <br><br>**Interesting:** Chained fully unauthenticated access to full admin, a database dump with plaintext passwords, and a fully instrumented SSRF oracle. | **Average.** Some raw evidence (live `debug_info`/`debug_data` extraction) but fewer full request/response captures than its zero-bank report. | **Good.** Backs the key chains with live debug output; the report claims end-to-end verified exploitation. | **Solid end-to-end run.** Demonstrates a complete compromise chain, though PoC depth is lighter than GLM's on the same target. |
| **GLM 5.2** | **14 Total** <br>(4 Critical, 6 High, 2 Medium, 2 Low) | **Highlights:** SSRF leaking the JWT signing key (`secret123`), DB password, and IMDS creds to forge an admin JWT; BOPLA self-registration as admin; full-PAN/CVV disclosure on virtual cards; negative-amount transfers. <br><br>**Interesting:** Documented Cloudflare rate-limiting (sqlmap drew 3,247 × 403) yet still landed every PoC via targeted manual requests. | **Excellent.** 18 HTTP blocks, 27 timestamps, and 33 raw `debug_info` captures, the most evidence-dense report on this target. | **High.** Exfiltrated live secrets and card data verbatim. | **Best-evidenced open-weight report on vulnbank.** Forensic-grade despite active WAF interference. |