services:
  kali-server:
    build:
      context: .
      dockerfile: Dockerfile.kali
    image: kali-mcp
    container_name: kali-mcp
    cap_add:
      - NET_RAW
      - NET_ADMIN
    environment:
      - COMMAND_TIMEOUT=1200
    ports:
      - "5000:5000"
    restart: unless-stopped

  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"
    restart: unless-stopped

  webui:
    build:
      context: .
      dockerfile: Dockerfile.opencode
    image: opencode-runner
    container_name: webui
    depends_on:
      - kali-server
      - ollama
    volumes:
      - ./results:/results
    working_dir: /results
    environment:
      # Ollama sign-in link (from `ollama signin`); shown as a "Log in" button when set.
      # The logged-in (green) state is detected live by the UI, not set here.
      - OLLAMA_LOGIN_URL=${OLLAMA_LOGIN_URL:-}
    entrypoint: ["python3", "/app/webui/server.py"]
    ports:
      - "8080:8080"
    restart: unless-stopped
