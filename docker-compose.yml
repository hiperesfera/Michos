services:
  kali-server:
    build:
      context: .
      dockerfile: Dockerfile.kali
    image: kali-mcp
    container_name: kali-mcp
    cap_add:
      - NET_RAW
      - NET_ADMIN
    environment:
      - COMMAND_TIMEOUT=600
    ports:
      - "5000:5000"
    restart: unless-stopped

  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"
    restart: unless-stopped

  webui:
    build:
      context: .
      dockerfile: Dockerfile.opencode
    image: opencode-runner
    container_name: webui
    depends_on:
      - kali-server
      - ollama
    volumes:
      - ./results:/results
      - ./webui:/webui
    working_dir: /results
    entrypoint: ["python3", "/webui/server.py"]
    ports:
      - "8080:8080"
    restart: unless-stopped
